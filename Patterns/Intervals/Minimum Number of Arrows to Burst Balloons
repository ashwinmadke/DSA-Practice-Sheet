https://leetcode.com/problems/minimum-number-of-arrows-to-burst-balloons/

TC - O(n log n)
SC - O(1)

public int findMinArrowShots(int[][] points) {
        // step 1 : sort based on end time
        Arrays.sort(points, (a,b)-> Integer.compare(a[1],b[1]));

        // 1st ballon ke liye 1 arrow to lgega he
        int arrow = 1;
        int end = points[0][1];// first ballon ka end

        for(int i=1; i<points.length; i++){
            if(points[i][0] > end){
                arrow++; // need new arrow
                end = points[i][1]; // update end
            }
            // no need to handle else case
        }
        return arrow;
    }
